namespace: nodejs
name: test
version: "1"
description: An example template for testing npm files
maintainer: foo@bar.com
config:
  image: node:8
  steps:
    - install: npm install
    - test: npm test
